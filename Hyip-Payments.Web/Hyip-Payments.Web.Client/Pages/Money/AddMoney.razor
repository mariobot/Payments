@page "/addmoney2"
@using System.Net.Http.Json
@using Hyip_Payments.Models
@inject HttpClient Http
@inject NavigationManager NavigationManager

<h3>Add Money</h3>

<EditForm Model="money" OnValidSubmit="HandleValidSubmit" FormName="addMoneyForm">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="mb-3">
        <label for="amount" class="form-label">Amount</label>
        <InputNumber id="amount" class="form-control" @bind-Value="money.Amount" />
    </div>
    <div class="mb-3">
        <label for="slug" class="form-label">Slug</label>
        <InputText id="slug" class="form-control" @bind-Value="money.Slug" />
    </div>
    <div class="mb-3">
        <label for="currency" class="form-label">Currency</label>
        <InputText id="currency" class="form-control" @bind-Value="money.Currency" />
    </div>
    <div class="mb-3">
        <label for="country" class="form-label">Country</label>
        <InputText id="country" class="form-control" @bind-Value="money.Country" />
    </div>
    <button type="submit" class="btn btn-primary">Add</button>
    <button type="button" class="btn btn-secondary" @onclick="GoBack">Cancel</button>
</EditForm>

@code {
    private MoneyModel money = new();

    private async Task HandleValidSubmit()
    {
        var response = await Http.PostAsJsonAsync("api/money", money);
        if (response.IsSuccessStatusCode)
        {
            NavigationManager.NavigateTo("/moneylist");
        }
        else
        {
            // Optionally handle error
        }
    }

    private void GoBack()
    {
        NavigationManager.NavigateTo("/moneylist");
    }
}
